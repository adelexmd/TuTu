# 2026-07-07

- 扫描 TuTu 项目整体结构与源码，核对 doc/tutu需求文档.md 中"已知问题"全部属实：ChatController 的 /explain 与 /explain1 重复、Assistant bean 未被使用、sendMessage 无 prompt 工程、API Key 未外部化、无 CORS、双前端未接入。记录了项目记忆（MEMORY.md）。
- 后端 P0 重构完成并编译通过：合并为 POST /api/v1/chat + POST /api/v1/chat/stream(SSE)，统一 Result{code,data,message} 返回，新增 CORS 配置、统一异常处理器，删除冗余 Assistant。关键坑：langchain4j 1.3.0 流式 API 已改为 `streamingChatModel.chat(String, StreamingChatResponseHandler)`，回调方法为 onPartialResponse(String)/onCompleteResponse(ChatResponse)/onError(Throwable)，handler 在 dev.langchain4j.model.chat.response 包（非旧版的 dev.langchain4j.model.output.StreamingResponseHandler）。用 JDK17+release17 在 Git Bash 经 `mvn.cmd`(Windows 路径)编译验证；本机无 JDK21。
